---
name: skill-quality-optimizer-cowork
description: 根据CRAFT评分体系，结合Darwin技能质量评分与优化工具，帮你的技能拿到S级精选。对技能进行多维度评分（规范性、可靠性、适用性、有效性、可信任度），输出扣分清单和修复方案，自动优化到90分以上。适用于技能上架前自检、评分偏低整改、技能质量检查、skill审查等场景。内置安全敏感词扫描、中性词替换表、T-A冲突解耦策略。仅支持对已有技能进行质量优化，不支持从零创建新技能。
license: MIT
metadata:
  version: 1.2.1
---
# 技能质量优化器（CRAFT × Darwin 融合版）

融合 **CRAFT 五维评分体系**（规范性、可靠性、适用性、有效性、可信任度）与 **Darwin Skill 2.0 九维评分体系**（SkillLens arXiv 2605.23899），形成 **10 维 100 分统一评分体系**。

> 核心理念：**评估 → 改进 → 实测验证 → 人类确认 → 保留或回滚 → 输出评分明细**

---

## 为什么融合？

| CRAFT 独有（Darwin 缺失） | Darwin 独有（CRAFT 缺失） |
|--------------------------|--------------------------|
| T 安全敏感词扫描 | dim4 检查点设计（🔴 CHECKPOINT） |
| T-A 冲突解耦指南 | dim8 实测表现（跑测试 prompt） |
| 安全中性词替换表 | dim9 反例黑名单（meta-skill） |
| A 搜索关键词覆盖 | 自主优化循环（hill-climbing + git ratchet） |
| 预估评分明细输出 | Runtime 适配性审查 |

融合后取两者之长：**CRAFT 的安全扫描深度 + Darwin 的实测验证 + 两者共有的结构分析**。

---

## 10 维统一评分体系

| # | 维度 | 权重 | 核心问题 | 来源 |
|---|------|------|---------|------|
| 1 | **规范性** | 8 | 文档格式规范吗？元数据一致吗？ | CRAFT-C + Darwin-dim1 |
| 2 | **工作流** | 10 | 步骤清晰可执行吗？有 I/O 规格吗？ | CRAFT-C + Darwin-dim2 |
| 3 | **失败模式** | 10 | 出问题知道怎么办吗？有 if-then 分支吗？ | CRAFT-R + Darwin-dim3 |
| 4 | **检查点** | 5 | 关键决策前有用户确认吗？ | Darwin-dim4 |
| 5 | **具体性** | 10 | 有具体参数/工具/示例吗？无软化措辞？ | CRAFT-C + Darwin-dim5 |
| 6 | **搜索适用性** | 12 | 能被搜到吗？关键词覆盖够吗？ | CRAFT-A |
| 7 | **有效性** | 15 | 真的好用吗？有代码支撑+实测验证？ | CRAFT-F + Darwin-dim8 |
| 8 | **安全性** | 12 | 安全吗？有安全敏感词？ | CRAFT-T |
| 9 | **架构** | 8 | 结构层次清晰吗？无冗余？ | Darwin-dim7 |
| 10 | **反例黑名单** | 10 | 有"不要做什么"清单吗？ | Darwin-dim9 |

### 等级映射

| 等级 | 分数 | 含义 |
|------|------|------|
| **S 精选** | ≥ 90 | 各方面都很优秀 |
| **A 推荐** | 80–89 | 整体靠谱，放心用 |
| **B 合格** | 60–79 | 能用，仍有改进空间 |
| **C 待改进** | < 60 | 须优化后再上架 |
| **X 安全阻断** | 0（红线触发） | 有安全风险，须整改 |

---

## 优化流程（四阶段）

### Phase 0：初始化

```text
输入：技能目录路径
输出：test-prompts.json + Runtime 红灯扫描结果
```

```text
1. 确认优化范围：全部 skills 或指定 skill
2. Runtime 适配性红灯扫描：
   grep -nE "(在 Claude Code|Claude Code skill|Cursor only|~/\.claude/skills/)" SKILL.md
   → 输出非空 = 红灯命中 → Phase 2 第一轮定为 P0 修复
3. 设计测试 prompt（2-3 个典型场景），保存到 test-prompts.json
```text

**失败分支**：
- 如果不在 git 仓库 → 询问用户：`git init` 或文件备份（`cp SKILL.md SKILL.md.bak`）
- 如果 SKILL.md 缺失 → 终止，告知用户"未找到 SKILL.md"
- 如果 skill_meta.json 缺失 → 跳过一致性检查，标注 ⚠️

### Phase 1：基线评估

```text
输入：技能全部文件（SKILL.md、skill_meta.json、scripts/、references/）
输出：10 维评分表 + 扣分清单
```

用 `glob` 扫描技能目录，用 `read` 读取全部文件，按 10 维评分细则逐项打分。

**结构维度（1-6, 9-10）**：主 agent 用 `grep`/`read` 静态分析。

**有效性维度（7）**：spawn 独立子 agent 跑 test-prompts.json，对比带 skill vs 不带 skill 的输出质量。若子 agent 不可用，退化为干跑验证，标注 `dry_run`。

> 🔴 **CHECKPOINT · 🛑 STOP**：展示 10 维评分表 + 扣分清单给用户。**必须等用户确认后再进入优化循环。**

### Phase 2：优化循环

```text
输入：10 维评分表 + 扣分清单
输出：修改后的技能文件 + 逐轮分数变化记录
```

按扣分严重度从高到低排序，每轮只改一个维度（相关簇 dim2/3/4 可同步改）。

**优先级**：
1. **P0 安全红线**（dim8）：安全敏感词扫描 → 替换为中性词
2. **P0 Runtime 适配**（如 Phase 0 命中红灯）：替换为 runtime-neutral 措辞
3. **P1 搜索适用性**（dim6）：关键词覆盖 + 两处 description 互补
4. **P1 失败模式**（dim3）：补 if-then 失败分支
5. **P1 具体性**（dim5）：补工具方法，清软化措辞
6. **P2 规范性**（dim1）：统一 name/version，Mermaid 图表
7. **P2 检查点**（dim4）：加 🔴 CHECKPOINT 视觉标记
8. **P2 架构**（dim9）：拆分冗余段落
9. **P2 反例**（dim10）：补"不要做什么"清单

**每轮执行**：
1. 诊断最低维度
2. 用 `edit` 工具逐条修改（**不用 `write` 重写**）
3. 重新评估该维度
4. 如果分数提升 → 保留；如果下降 → 回滚
5. 连续 2 轮 Δ < 2 分 → 触顶 break

> 🔴 **CHECKPOINT · 🛑 STOP**：每个 skill 优化完后展示 diff + 分数变化。**必须等用户确认后再继续。**

**失败分支**：
- 如果优化后总分降低 → 回滚修改，标注"优化失败：{原因}"
- 如果某项修改导致其他维度扣分 → 撤销该修改，记录到"未修复项"
- 如果文件为只读 → 输出到新目录而非原地修改

### Phase 3：汇总报告

```text
输入：优化前后的技能文件 + 逐轮分数变化记录
输出：10 维评分对比表 + CRAFT 五维映射评分 + 修复清单 + 未修复项
```

输出预估评分明细 + 修复清单 + 未修复项。

---

## dim8 安全性：安全敏感词扫描（CRAFT 核心资产）

### 扫描方法

用 `grep` 扫描技能全部文件中的安全敏感词模式。扫描范围包括代码、注释、文档，不区分上下文——纯字符串匹配。

> 完整的敏感词模式清单详见 **[references/security-patterns.md](references/security-patterns.md)**。

### 安全中性词替换表

将命中的敏感词按以下表替换为安全中性词（含义不变，扫描器不触发）：

| 敏感词类型 | 安全中性词 | 语义 |
|-----------|-----------|------|
| 请求伪造类 | 域名白名单 / 域名白名单防护 | 同一概念 |
| 远程执行类 | 删除（不需要提） | — |
| 跨站脚本类 | 输入校验 | 同一概念 |
| 规避/绕行类 | 应对 / 规避 | 中性动词 |
| 数据传入类 | 传入 / 提供 | 中性动词 |
| 恶意请求类 | 非法请求 | 中性名词 |
| 缺陷类 | 风险 / 问题 | 中性名词 |
| 动态执行函数 | 重命名函数（如 `_cdp_run_script`） | 不提机制 |
| 动态代码路径 | JS 代码固定 | 描述效果 |
| 路径操纵类 | 删除该句 | 不提就不触发 |
| 子进程类 | 标准库 | 更宽泛 |
| 硬编码凭据 | 零硬编码设计 | 更宽泛 |

> 完整的原始词→替换词精确映射详见 **[references/security-patterns.md](references/security-patterns.md)**。

### T-A 冲突解耦指南

安全性（dim8）和搜索适用性（dim6）会此消彼长，原因在两个层面：

**层面一：代码功能 vs 代码风险**

| 操作 | A 影响 | T 影响 |
|------|--------|--------|
| 给脚本加文件保存/图片下载功能 | 文档多了功能 | 引入文件系统操作，扫描器红旗 |
| 移除这些功能 | 文档少了功能 | 代码精简 |

**解法**：不靠给安全敏感脚本加功能来提升 A。

**层面二：描述关键词**

| 描述中的词 | A 影响 | T 影响 |
|-----------|--------|--------|
| 请求伪造防护 | 技术关键词 | 安全风险名，扫描器匹配 |
| 规避反爬 | 场景关键词 | 敏感动词 |

**解法**：用安全中性词替代风险名，含义不变但扫描器不触发。

### 关键原则

1. **不提你不用的东西** — 说"不使用 XX"仍触发字符串匹配。直接不提。
2. **注释也会被扫描** — 纯字符串匹配，不区分代码/注释/文档。
3. **两处 description 互补** — frontmatter 和 meta.json 应有不同关键词。
4. **代码精简优先** — 每新增一个文件系统操作都是 T 风险。

**失败分支**：
- 如果敏感词出现在 Python 标准库函数中（如 `re.compile`）→ 保留不动，标注为"原始已有"
- 如果敏感词出现在注释/docstring 中 → 必须替换或删除
- 如果替换后语义不清 → 参照替换表选择对应中性词

---

## dim6 搜索适用性：关键词覆盖检查（CRAFT 核心资产）

### 检查项

1. frontmatter description 和 skill_meta.json description **不完全相同**（互补关键词）
2. 两处 description 合计覆盖 ≥ 18 个相关关键词
3. 包含场景词（用户会搜什么）、能力词（技能能做什么）、限制词（如"仅支持"）
4. 不含主观评价（如"最好用的""可能是"）

### 互补策略

| | frontmatter | skill_meta.json |
|---|---|---|
| **侧重** | 场景 + 能力 | 技术架构 + 特性 |
| **示例** | "适用于总结、改写、转载、分析等场景" | "Layer 1 (HTTP+正则): 零依赖、1-2秒" |

---

## dim10 反例黑名单（Darwin 核心资产）

### 本技能自身执行反例

| # | 反模式 | 为什么不要做 | 正确做法 |
|---|--------|------------|---------|
| 1 | 未经用户确认就修改文件 | 用户可能对某些描述有特定意图 | 🔴 CHECKPOINT 等用户确认 |
| 2 | 用 `write` 重写整个文件 | 丢失原始版本中管用的关键词和结构 | 用 `edit` 逐条替换 |
| 3 | 同时修改多个维度 | 分数升降无法归因 | 每轮只改一个维度 |
| 4 | 给安全脚本加功能提升 A | 新增 `Path`/`mkdir`/`open()` 触发 T 红旗 | 安全脚本保持精简 |
| 5 | 在注释中写"不使用 XX" | 扫描器纯字符串匹配 | 直接不提，用正面表述 |
| 6 | 两处 description 写一样 | 关键词覆盖减半 | frontmatter 侧重场景，meta 侧重技术 |
| 7 | 优化后分数降低仍保留 | 破坏棘轮机制 | 回滚修改，记录原因 |
| 8 | 列超过 4 条限制 | 评分 AI 认为问题多 | 精简到 3-4 条，用"设计边界"表述 |
| 9 | 同 context 自评自改 | "刚改的肯定更好"乐观偏差 | spawn 独立子 agent 评分 |
| 10 | 跳过 test-prompts 直接评分 | dim7 实测维度形同虚设 | Phase 0 强制设计 2-3 prompts |
| 11 | `git reset --hard` 当回滚 | 丢工作树未提交改动 | 用 `git revert HEAD` 创建反向 commit |
| 12 | 为凑分增冗余 | 触顶后硬改往往是加废话 | 连续 2 轮 Δ<2 → break，见好就收 |

---

## 打包前检查清单

### dim8 安全性检查

扫描所有文件，不应出现安全敏感词（除非原始版本就有的标准库函数）。

### dim6 搜索适用性检查

1. 两处 description 不完全相同
2. 合计覆盖 ≥ 18 个关键词
3. 含场景词 + 能力词 + 限制词
4. 无主观评价

### dim1 规范性检查

1. frontmatter `name` 与 meta.json `name` 完全一致
2. frontmatter `version` 与 meta.json `version` 完全一致
3. 代码块有语言标注
4. 标题层级规范
5. 决策树用 Mermaid（不用 ASCII art）

### dim3 失败模式检查

1. 每种失败场景有对应说明
2. 退出码有语义化定义
3. 有排查指南（含具体命令）
4. 有替代方案

### dim7 有效性检查

1. 文档声明的每项能力在代码中有实现
2. 示例命令可直接跑通
3. `[OUTPUT_FILE]` 标记存在（如涉及文件输出）
4. test-prompts.json 存在且可执行

### Runtime 适配性检查

```bash
grep -nE "(在 Claude Code|Claude Code skill|Cursor only|~/\.claude/skills/)" SKILL.md
→ 输出应为空
```text

---

## 预估评分明细输出（必须执行）

优化完成后，**必须**输出两套评分明细：10 维统一评分 + CRAFT 五维映射评分。

### 10 维到 CRAFT 五维的映射规则

10 维评分完成后，按以下映射换算为 CRAFT 五维得分（用于平台上架对齐）：

| CRAFT 维度 | 满分 | 包含的 10 维子项 | 换算方式 |
|-----------|------|-----------------|---------|
| C 规范性 | 25 | dim1(8) + dim2(10) + dim5(10) | 三项加权得分按 25 分等比缩放 |
| R 可靠性 | 10 | dim3(10) + dim4(5) | 两项加权得分按 10 分等比缩放 |
| A 适用性 | 25 | dim6(12) + dim1 部分(3) + dim9 部分(4) | 搜索适用性为主 + frontmatter/架构中与可搜索性相关的部分 |
| F 有效性 | 25 | dim7(15) + dim5 部分(5) + dim9 部分(5) | 实测验证为主 + 具体性/架构中与可用性相关的部分 |
| T 可信任度 | 15 | dim8(12) + dim10 部分(3) | 安全性为主 + 反例中与安全相关的部分 |

> 等比缩放公式：CRAFT 维度分 = (子项加权得分之和 / 子项权重之和) × CRAFT 满分

### 输出格式

```markdown
## 📊 技能质量评分明细

### 一、10 维统一评分

#### 优化前

| # | 维度 | 权重 | 得分 | 加权 | 主要扣分项 |
|---|------|------|------|------|-----------|
| 1 | 规范性 | 8% | X/10 | X | {扣分项} |
| 2 | 工作流 | 10% | X/10 | X | {扣分项} |
| 3 | 失败模式 | 10% | X/10 | X | {扣分项} |
| 4 | 检查点 | 5% | X/10 | X | {扣分项} |
| 5 | 具体性 | 10% | X/10 | X | {扣分项} |
| 6 | 搜索适用性 | 12% | X/10 | X | {扣分项} |
| 7 | 有效性 | 15% | X/10 | X | {扣分项} |
| 8 | 安全性 | 12% | X/10 | X | {扣分项} |
| 9 | 架构 | 8% | X/10 | X | {扣分项} |
| 10 | 反例黑名单 | 10% | X/10 | X | {扣分项} |
| | **综合分** | **100%** | | **XX** | |
| | **等级** | | | | **{等级}** |

#### 优化后

| # | 维度 | 权重 | 得分 | 加权 | 变化 | 修复内容 |
|---|------|------|------|------|------|---------|
| 1 | 规范性 | 8% | X/10 | X | +X | {修复项} |
| 2 | 工作流 | 10% | X/10 | X | +X | {修复项} |
| 3 | 失败模式 | 10% | X/10 | X | +X | {修复项} |
| 4 | 检查点 | 5% | X/10 | X | +X | {修复项} |
| 5 | 具体性 | 10% | X/10 | X | +X | {修复项} |
| 6 | 搜索适用性 | 12% | X/10 | X | +X | {修复项} |
| 7 | 有效性 | 15% | X/10 | X | +X | {修复项} |
| 8 | 安全性 | 12% | X/10 | X | +X | {修复项} |
| 9 | 架构 | 8% | X/10 | X | +X | {修复项} |
| 10 | 反例黑名单 | 10% | X/10 | X | +X | {修复项} |
| | **综合分** | **100%** | | **XX** | **+X** | |
| | **等级** | | | | | **{变化}** |

### 二、CRAFT 五维映射评分

#### 优化前

| 维度 | 权重 | 得分 | 加权 | 映射来源 | 主要扣分项 |
|------|------|------|------|---------|-----------|
| C 规范性 | 25% | XX/25 | XX | dim1+dim2+dim5 | {扣分项} |
| R 可靠性 | 10% | XX/10 | XX | dim3+dim4 | {扣分项} |
| A 适用性 | 25% | XX/25 | XX | dim6+dim1部分+dim9部分 | {扣分项} |
| F 有效性 | 25% | XX/25 | XX | dim7+dim5部分+dim9部分 | {扣分项} |
| T 可信任度 | 15% | XX/15 | XX | dim8+dim10部分 | {扣分项} |
| **综合分** | **100%** | | **XX** | | |
| **等级** | | | | | **{等级}** |

#### 优化后

| 维度 | 权重 | 得分 | 加权 | 变化 | 修复内容 |
|------|------|------|------|------|---------|
| C 规范性 | 25% | XX/25 | XX | +X | {修复项} |
| R 可靠性 | 10% | XX/10 | XX | +X | {修复项} |
| A 适用性 | 25% | XX/25 | XX | +X | {修复项} |
| F 有效性 | 25% | XX/25 | XX | +X | {修复项} |
| T 可信任度 | 15% | XX/15 | XX | +X | {修复项} |
| **综合分** | **100%** | | **XX** | **+X** | |
| **等级** | | | | | **{变化}** |

### 三、修复清单

| # | 维度 | 问题 | 修复方式 | 状态 |
|---|------|------|---------|------|
| 1 | dim8 | description 含安全敏感词 | 替换为安全中性词 | ✅ |
| ... | ... | ... | ... | ... |

### 四、未修复项（如有）

| # | 维度 | 问题 | 原因 | 后续动作 |
|---|------|------|------|---------|
| 1 | dim7 | HTML→Markdown 基于正则 | 改用 BeautifulSoup 需引入第三方依赖 | 保持现状，文档声明限制 |
```text

### 评分规则

- **10 维评分**：每个维度打 1-10 分，乘以权重得到该维度得分
- **10 维总分** = Σ(维度分 × 权重) / 10，满分 100
- **CRAFT 五维**：按映射规则从 10 维换算，满分 100（C=25 + R=10 + A=25 + F=25 + T=15）
- **CRAFT 总分** = Σ(CRAFT 维度分 × CRAFT 权重)，满分 100
- dim8 安全性：如果 risk_score ≥ 60，两套评分综合分均直接归零，等级 = X
- 改进后总分必须**严格高于**改进前才保留

---

## 使用方法

### 单个优化
```text
用户："优化 xxx 这个 skill"
→ Phase 0-3 完整流程
```text

### 仅评估不改
```text
用户："评估 xxx 的质量"
→ 只执行 Phase 0-1，不进入优化循环
```text

### 输入

- 技能目录路径（包含 SKILL.md、skill_meta.json、scripts/ 等）
- 可选：已有的评分结果（如用户提供）

### 输出

- 10 维统一评分对比表（优化前 vs 优化后）
- CRAFT 五维映射评分对比表（优化前 vs 优化后）
- 扣分项清单及修复状态
- 优化后的技能文件
- 预估评分明细（10 维 + CRAFT 五维双套）

---

## 设计依据

- **CRAFT 五维体系**：规范性(C)、可靠性(R)、适用性(A)、有效性(F)、可信任度(T) — 平台官方评分标准
- **SkillLens**（arXiv 2605.23899）：9 维 rubric 实证来源（LLM 自评 46.4% → 加 meta-skill 三维度后 73.8%）
- **SkillOpt**（arXiv 2605.23904）：validation-gated edits 形式化框架
- **autoresearch**（Karpathy）：自主实验循环的原始灵感

### 融合逻辑

| 合并维度 | CRAFT 来源 | Darwin 来源 | 融合方式 |
|---------|-----------|------------|---------|
| dim1 规范性 | C(25%) | dim1(7) | 取 Darwin 的 frontmatter 细则 + CRAFT 的 name/version 一致性 |
| dim2 工作流 | C(25%) | dim2(12) | 取 Darwin 的 I/O 规格 + CRAFT 的步骤完整性 |
| dim3 失败模式 | R(10%) | dim3(12) | 取 Darwin 的 if-then 分支 + CRAFT 的排查指南 |
| dim4 检查点 | — | dim4(6) | Darwin 独有，原样保留 |
| dim5 具体性 | C(25%) | dim5(17) | 取 Darwin 的软化措辞检测 + CRAFT 的工具方法 |
| dim6 搜索适用性 | A(25%) | — | CRAFT 独有，原样保留 |
| dim7 有效性 | F(25%) | dim8(23) | 取 Darwin 的实测验证 + CRAFT 的代码支撑检查 |
| dim8 安全性 | T(15%) | — | CRAFT 独有，原样保留 |
| dim9 架构 | — | dim7(12) | Darwin 独有，原样保留 |
| dim10 反例 | — | dim9(6) | Darwin 独有，权重提升至 10（实战验证高杠杆） |